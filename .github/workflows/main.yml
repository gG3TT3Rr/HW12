name: Continuous Integration

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
          node-version: '16'
      
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x
        node-version: '16'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run unit tests
      run: python test.py
      
    - name: Update repository
      run: |
        git config user.name "gG3TT3Rr"
        git config user.email "knvlvnkt@gmail.com"
        git commit -am "Update project"
        git push
        
    - name: Show workflow link
      run: echo "Workflow executed. View details at ${{github.event.workflow_run_url}}"

